# Lab 08: Wazuh Monitoring Server Setup

## Overview

This lab documents the deployment and configuration of a Wazuh monitoring server for the Project Athenaeum Business Guardian environment.

The Wazuh server was created as the centralized security-monitoring system for the isolated `BusinessGuardianLab` network. It will eventually collect and analyze endpoint telemetry, security events, system logs, file-integrity activity, authentication events, and other information generated by the simulated Windows business workstation.

The server uses two virtual network adapters. One adapter provides temporary outbound connectivity for installation and updates, while the second adapter connects the server to the isolated Business Guardian lab network.

## Objective

Deploy and configure a Wazuh monitoring server that can securely communicate with future monitored endpoints inside the isolated `BusinessGuardianLab` environment while allowing controlled dashboard access from the host computer.

## Skills Demonstrated

- Virtual machine deployment
- Linux server administration
- Wazuh installation and configuration
- SIEM and endpoint-monitoring fundamentals
- Dual-network adapter configuration
- Static IPv4 addressing
- Linux network-interface identification
- Network connectivity validation
- VirtualBox NAT port forwarding
- Web-dashboard access
- Service-status verification
- Troubleshooting
- Credential protection
- Technical documentation
- Privacy-conscious evidence handling

## Environment and Tools

- Windows 11 host computer
- Oracle VirtualBox
- Linux-based Wazuh server virtual machine
- Wazuh
- VirtualBox NAT networking
- VirtualBox Internal Network
- Internal network named `BusinessGuardianLab`
- Linux command line
- Network-management utilities
- Web browser
- HTTPS dashboard access

## Lab Systems

### Wazuh Monitoring Server

The Wazuh server is the centralized monitoring system for the Business Guardian environment.

Internal lab address:

```text
192.168.70.20/24
```

### BusinessGuardian Windows Workstation

The Windows 11 business workstation created in Lab 07 uses:

```text
192.168.70.10/24
```

The workstation and Wazuh server share the isolated VirtualBox Internal Network named:

```text
BusinessGuardianLab
```

## Network Architecture

The Wazuh server uses two enabled network adapters.

### Adapter 1 — NAT

The first adapter provides controlled outbound connectivity for installation, package retrieval, updates, and administrative setup.

Linux interface:

```text
enp0s3
```

Assigned NAT address:

```text
10.0.2.15/24
```

### Adapter 2 — Internal Network

The second adapter connects the Wazuh server to the isolated Business Guardian environment.

Linux interface:

```text
enp0s8
```

Static internal address:

```text
192.168.70.20/24
```

VirtualBox network name:

```text
BusinessGuardianLab
```

No Bridged Adapter was used.

## Network Topology

```text
Windows 11 Host
        |
 Oracle VirtualBox
        |
        +----------------------------------+
        |                                  |
   NAT Network                    Internal Network
   Installation and               BusinessGuardianLab
   update access                   192.168.70.0/24
        |                                  |
  Wazuh Server                     +----------------------+
  enp0s3                           |                      |
  10.0.2.15/24              Wazuh Server          Windows Workstation
                            enp0s8                192.168.70.10/24
                            192.168.70.20/24
```

## Work Completed

During this lab, I:

- Created the Wazuh monitoring-server virtual machine
- Configured the virtual-machine hardware and installation media
- Installed the Linux server operating system
- Completed the initial operating-system setup
- Verified the server’s network interfaces
- Confirmed that Adapter 1 used NAT
- Identified the NAT interface as `enp0s3`
- Verified the NAT address `10.0.2.15/24`
- Enabled Adapter 2 in VirtualBox
- Connected Adapter 2 to the Internal Network named `BusinessGuardianLab`
- Identified the internal interface as `enp0s8`
- Configured the persistent static address `192.168.70.20/24`
- Verified the completed dual-interface configuration
- Confirmed that no Bridged Adapter was enabled
- Installed the Wazuh server components
- Verified Wazuh-related services
- Configured VirtualBox NAT port forwarding for dashboard access
- Accessed the Wazuh dashboard from the host computer
- Confirmed that the dashboard login page loaded successfully
- Protected screenshots containing generated credentials
- Documented installation, networking, troubleshooting, and validation results

## Static Internal Address Configuration

The internal Wazuh interface was configured with:

```text
Interface: enp0s8
Address: 192.168.70.20/24
Network: 192.168.70.0/24
```

A predictable static address is required because monitored endpoints must know where to send their security data.

The Wazuh server and Windows workstation now occupy the same isolated subnet:

```text
Wazuh server:        192.168.70.20
Windows workstation: 192.168.70.10
```

## Dashboard Access

The Wazuh dashboard was made accessible from the Windows host through a VirtualBox NAT port-forwarding rule.

The dashboard was reached through the host browser at:

```text
https://127.0.0.1:8443
```

The browser displayed a security warning because the lab dashboard used a locally generated certificate. Access remained limited to the local host and authorized virtual environment.

The successful login-page display confirmed that:

- The Wazuh dashboard service was running
- The NAT port-forwarding rule was functioning
- The host browser could reach the dashboard
- The server installation had progressed successfully

## Service and System Validation

The server configuration was reviewed using Linux networking and service-management commands.

Validation included:

- Confirming both enabled network interfaces
- Verifying IPv4 addresses
- Reviewing interface status
- Confirming the internal static address
- Reviewing Wazuh-related service status
- Confirming dashboard availability
- Verifying that the host port-forwarding rule reached the virtual machine

These checks helped distinguish network problems from application-service problems during troubleshooting.

## Troubleshooting

The lab required troubleshooting several areas, including:

- Identifying the correct Linux interface names
- Distinguishing the NAT adapter from the internal adapter
- Assigning a persistent address to `enp0s8`
- Verifying that the internal network name matched exactly
- Confirming that the virtual cable was connected
- Reaching the Wazuh dashboard from the host browser
- Configuring the correct host and guest ports
- Interpreting the browser certificate warning
- Protecting installation credentials from public evidence

The troubleshooting process reinforced the importance of validating one layer at a time:

1. VirtualBox adapter configuration
2. Linux interface configuration
3. IP addressing
4. Service status
5. Port forwarding
6. Browser access

## Credential and Evidence Protection

The Wazuh installation generated administrative credentials that must not be published.

One screenshot containing potentially sensitive Wazuh-generated credentials was retained privately and excluded from GitHub.

Public portfolio evidence must not expose:

- Wazuh administrator passwords
- Generated installation credentials
- Recovery information
- API keys or tokens
- Private certificates or keys
- Personal account information
- Host-system identifying information

Sensitive screenshots are retained only in the private local Project Athenaeum documentation.

## Security and Safety Boundaries

This lab follows these safety rules:

- All systems are personally owned and authorized
- The Wazuh server uses the isolated `BusinessGuardianLab` network for endpoint communication
- No Bridged Adapter is enabled
- The NAT adapter is used only for controlled installation and administration
- Dashboard access is forwarded only to the local host
- No real customer, financial, City, employer, or production data is used
- Future business activity will be synthetic
- Credentials are excluded from public screenshots
- Snapshots and documentation are created before major changes
- Monitoring is limited to authorized Project Athenaeum systems

## Importance

Centralized monitoring is an essential component of modern security operations.

A Wazuh server can collect endpoint data, detect suspicious activity, correlate events, support file-integrity monitoring, review authentication activity, and provide analysts with a central dashboard for investigation.

This lab established the monitoring infrastructure required for later Business Guardian exercises involving:

- Windows endpoint telemetry
- Security-event collection
- File-integrity monitoring
- Authentication monitoring
- Controlled alert generation
- Incident investigation
- Python-based correlation
- AI-assisted alert explanation

## Lessons Learned

This lab demonstrated that deploying a monitoring platform requires more than installing software.

The network architecture must allow the server to perform administrative tasks while also maintaining a trusted path to monitored endpoints. Using separate NAT and internal adapters provides a practical way to separate those functions.

The lab also reinforced the value of methodical troubleshooting. Verifying VirtualBox settings, Linux interfaces, IP addresses, services, port-forwarding rules, and browser access individually made it easier to locate and correct configuration problems.

Finally, the lab reinforced that security documentation must protect credentials. Evidence should prove that work was completed without publishing information that could compromise the environment.

## Documentation Created

The following Lab 08 documentation was completed and retained locally:

- Detailed Lab 08 technical notes
- Lab 08 screenshot log
- Wazuh installation and configuration evidence
- Network-interface evidence
- Dashboard-access evidence
- Troubleshooting notes
- Private credential-containing evidence excluded from GitHub
- Final portfolio documentation

## Future Development

The next Business Guardian phase will include:

- Installing the Wazuh agent on the Windows workstation
- Registering the Windows workstation with the Wazuh server
- Confirming active endpoint reporting
- Reviewing Windows security and system events
- Generating controlled test alerts
- Reviewing Wazuh alert details
- Documenting agent-management and troubleshooting steps
- Creating sanitized portfolio evidence

## Status

**Technical configuration completed and GitHub evidence upload in progress**
